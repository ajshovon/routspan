# Fixtures

Raw JSON responses captured from real devices, used as regression fixtures for parsers (SMS
decoding, status parsing, etc).

**Redact before committing:** IMEI, IMSI, ICCID, phone numbers, MAC addresses, WiFi passwords, and
any SMS content. Replace with obviously-fake placeholder values (see existing tests for the
pattern) rather than real captured data.
